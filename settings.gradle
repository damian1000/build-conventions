plugins {
    // Provisions the JDK 25 toolchain on machines/builders that lack it (e.g. JitPack).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'build-conventions'
